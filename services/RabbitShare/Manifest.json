{
    "commands" : {
        "plant_flag"  : "%SERVICE_DIRECTORY%/check_rabbitshare -p %HOST% %FLAGID% %FLAG%",
        "check_flag"  : "%SERVICE_DIRECTORY%/check_rabbitshare -c %HOST% %FLAGID% %FLAG%",
        "exploitable" : "%SERVICE_DIRECTORY%/check_rabbitshare -e %HOST%"
    }
}
