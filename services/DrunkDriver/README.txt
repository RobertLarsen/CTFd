Install on CTF server
---------------------
1) Install required packages:
    apt-get -y install make gcc sqlite3

2) Install module and script:
    make install_drunkdriver

3) Insert module:
    modprobe drunkdriver
